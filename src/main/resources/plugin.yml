name: SafeStop
version: ${project.version}
main: reloc.ss.net.dohaw.safestop.SafeStop
api-version: 1.16

commands:
  safestop:
    permission: safestop.use
    aliases:
      - safes
      - sstop
      - ssto
      - safestop

permissions:
  safestop.use:
    default: op
